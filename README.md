# Book Collection App

Este proyecto es una aplicación web para gestionar tu colección de libros, desarrollada con React y Vite.

This project is a web application to manage your book collection, developed with React and Vite.

## Demo
Netlify aquí:  
[https://super-taiyaki-49a287.netlify.app/](https://super-taiyaki-49a287.netlify.app/)

Netlify demo here:  
[https://super-taiyaki-49a287.netlify.app/](https://super-taiyaki-49a287.netlify.app/)

## Requisitos previos

- Node.js (v20 o superior recomendado)
- npm

## Prerequisites

- Node.js (v20 or higher recommended)
- npm

## Instalación

1. Clona este repositorio:
   ```sh
   git clone https://github.com/tu-usuario/tu-repo.git
   cd 11_client-side_final-project_frontend
   ```

2. Instala las dependencias:
   ```sh
   npm install
   ```

## Installation

1. Clone this repository:
   ```sh
   git clone https://github.com/your-username/your-repo.git
   cd 11_client-side_final-project_frontend
   ```

2. Install dependencies:
   ```sh
   npm install
   ```

## Uso en desarrollo

Para iniciar el servidor de desarrollo:

```sh
npm run dev
```

La aplicación estará disponible en [http://localhost:5173](http://localhost:5173) (o el puerto que indique la terminal).

## Development usage

To start the development server:

```sh
npm run dev
```

The app will be available at [http://localhost:5173](http://localhost:5173) (or the port shown in the terminal).

## Scripts disponibles

- `npm run dev` — Inicia el servidor de desarrollo.
- `npm run build` — Genera la versión de producción en la carpeta `dist`.
- `npm run preview` — Sirve la versión de producción localmente.
- `npm run lint` — Ejecuta ESLint para comprobar el código.
- `npm run test` — Ejecuta los tests con Vitest.

## Available scripts

- `npm run dev` — Starts the development server.
- `npm run build` — Builds the production version in the `dist` folder.
- `npm run preview` — Serves the production build locally.
- `npm run lint` — Runs ESLint to check the code.
- `npm run test` — Runs tests with Vitest.

## Variables de entorno

Puedes configurar la URL de la API en el archivo `.env`:

```
VITE_API_URL = "http://localhost:3000/books"
```

## Environment variables

You can set the API URL in the `.env` file:

```
VITE_API_URL = "http://localhost:3000/books"
```

## Testing

Para ejecutar los tests:

```sh
npm run test
```

## Testing

To run the tests:

```sh
npm run test
```

---

Pau!

Developed by Pau.
